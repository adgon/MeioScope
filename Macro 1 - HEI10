//Split channels
run("Duplicate...", "duplicate");
run("Z Project...", "projection=[Max Intensity]");
run("Split Channels");
run("Duplicate...", " ");
 
// Measure intensity under Yen thershold in HEI10 channel
setAutoThreshold("Yen dark");
run("Measure");
run("Create Selection");
roiManager("Add");
roiManager("Select", 0);
roiManager("Rename", "HEI10 Yen");
close();

// Measure intensity under Moments thershold in HEI10 channel
setAutoThreshold("Yen dark");
setAutoThreshold("Moments dark");
roiManager("Select", 0);
run("Create Selection");
roiManager("Add");
roiManager("Select", 1);
roiManager("Rename", "HEI10 Moments");
run("Measure");
close();

// Set ZYP1 labeling as a Region of Interest
run("Duplicate...", " ");
setAutoThreshold("Default dark");
run("Create Selection");
roiManager("Add");
roiManager("Select", 2);
roiManager("Rename", "Zyp1 Default");
close();

// Navigate through channels
close();
close();
close();

// Count HEI10 foci overlapping with ZYP1
roiManager("Show All");
roiManager("Select", 0);
roiManager("Select", newArray(0,1));
roiManager("Select", newArray(0,1,2));
roiManager("AND");
roiManager("Add");
roiManager("Select", 3);
roiManager("Rename", "overlap");
RoiManager.setGroup(0);
RoiManager.setPosition(0);
roiManager("Set Color", "blue");
roiManager("Set Line Width", 0);
run("Z Project...", "projection=[Max Intensity]");
run("Split Channels");
setAutoThreshold("Yen dark");
run("Convert to Mask");
roiManager("Select", 3);
run("Analyze Particles...", "summarize");

// Close everything
close();
close();
close();
close();
close();
close();
roiManager("Delete");
